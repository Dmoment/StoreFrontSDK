# frozen_string_literal: true

RSpec.describe Vendo do
  it "has a version number" do
    expect(Vendo::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
